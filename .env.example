# Copy to .env and fill in your Claude API key.
# Get one at console.anthropic.com → API keys.
# Without a key the app runs fine — Focus mode shows curated static picks.
CLAUDE_API_KEY=sk-ant-api03-...
